# My Laravel App with OpenTelemetry

This repository contains a Laravel app Dockerized with OpenTelemetry auto-instrumentation.

## Build Docker image

```bash
docker build --target prod -t my-laravel-app:prod .

